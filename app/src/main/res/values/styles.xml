<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Set the default button style -->
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <!--<item name="android:background">@drawable/button_backgound</item>-->
        <item name="android:textColor">@color/screen_background_color4</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:radius">10dp</item>
    </style>
    <!-- Your custom button style -->
        <!-- Add more attributes if you need -->
</resources>